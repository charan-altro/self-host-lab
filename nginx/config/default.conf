server {
    listen 80;
    server_name _;

    # Standard root
    root /usr/share/nginx/html;
    index index.html;

    # Standard fallback
    location / {
        try_files $uri $uri/ =404;
    }
}
